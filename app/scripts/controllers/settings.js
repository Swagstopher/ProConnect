'use strict';

/**
 * @ngdoc function
 * @name proConnectApp.controller:SettingsCtrl
 * @description
 * # SettingsCtrl
 * Controller of the proConnectApp
 */
angular.module('proConnectApp')
  .controller('SettingsCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
